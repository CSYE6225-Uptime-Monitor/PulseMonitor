user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log;
pid /run/nginx.pid;

include /usr/share/nginx/modules/*.conf;

events {
  worker_connections 1024;
}

http {
  log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                  '$status $body_bytes_sent "$http_referer" '
                  '"$http_user_agent" "$http_x_forwarded_for"';

  access_log /var/log/nginx/access.log main;

  sendfile            on;
  tcp_nopush          on;
  keepalive_timeout   65;
  types_hash_max_size 4096;

  include /etc/nginx/mime.types;
  default_type application/octet-stream;

  # proxy_set_header REPLACES rather than appends, and nginx itself always
  # terminates on plaintext :80 - so `X-Forwarded-Proto $scheme` here would
  # always be "http", overwriting the ALB's correct "https" and never the
  # other way around. Once cookie_secure is true, that broken header makes
  # cookie-session's underlying Cookies lib see req.protocol !== 'https' and
  # throw "Cannot send secure cookie over unencrypted connection" on every
  # request that touches the session (csrf.js writes to it on nearly all of
  # them) - a silent full outage that /healthz (session-free) would not catch.
  # Passing through the inbound header when present preserves the ALB's value.
  map $http_x_forwarded_proto $forwarded_proto {
    default $http_x_forwarded_proto;
    ''      $scheme;
  }

  # Single server block, no conf.d/ fragments: AL2023's stock nginx.conf
  # ships its own `server { listen 80 default_server; }`, and a second
  # default_server in conf.d/ refuses to start while a non-default one gets
  # shadowed - either way the ALB ends up hitting nginx's welcome page
  # instead of the app, and /healthz 404s forever.
  server {
    listen 80 default_server;
    server_name _;

    # ALB health check target - must stay unprefixed so the target group's
    # /healthz check keeps working unchanged.
    location /healthz {
      proxy_pass         http://127.0.0.1:8080;
      proxy_http_version 1.1;
      proxy_set_header   Host              $host;
      proxy_set_header   X-Real-IP         $remote_addr;
      proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
      proxy_set_header   X-Forwarded-Proto $forwarded_proto;
    }

    # Trailing slash on proxy_pass strips the /api prefix, so the frontend's
    # same-origin /api/v1/... calls (see frontend/next.config.ts's dev-time
    # rewrite) reach Express as /v1/... in production too.
    location /api/ {
      proxy_pass         http://127.0.0.1:8080/;
      proxy_http_version 1.1;
      proxy_set_header   Host              $host;
      proxy_set_header   X-Real-IP         $remote_addr;
      proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
      proxy_set_header   X-Forwarded-Proto $forwarded_proto;
    }

    location / {
      proxy_pass         http://127.0.0.1:3000;
      proxy_http_version 1.1;
      proxy_set_header   Host              $host;
      proxy_set_header   X-Real-IP         $remote_addr;
      proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
      proxy_set_header   X-Forwarded-Proto $forwarded_proto;
    }
  }
}
