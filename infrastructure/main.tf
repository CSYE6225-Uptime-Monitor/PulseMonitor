# PulseMonitor root module.
#
# This init sprint establishes structure only — no resources are provisioned yet.
# Module wiring is added (uncommented) as each layer is implemented in later
# sprints, following the dependency order:
#
#   network -> compute / storage -> monitoring -> dns
#
# See ./modules/* for the per-layer scaffolds and infrastructure/README.md for
# the roadmap.
